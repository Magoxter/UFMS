    /* MGA */
#include <stdio.h>
#include <string.h>
#define MAX 100

int main (void) {
  int    n,i,
         notas[MAX];
  double soma,mga;
   soma = 0.0;
    scanf("%d", &n);
   for (i = 0; i < n; i++)
     scanf("%d", &notas[i]);
   for (i = 0; i < n; i++)
     soma += v[i];
    mga = soma / n;
   printf("%lf\n", mga);
 return 0;
}
